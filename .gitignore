# The Kaggle metadata dump, 5.5 GB. Needed only to backfill a new category.
arxiv-metadata-oai-snapshot.json

# Model weights live in ~/.cache/huggingface and Ollama's store, never here.
models/
*.gguf
*.safetensors

# Run output
*.log
gpucore.*

# Python
__pycache__/
*.py[cod]
.venv/
build/
dist/
*.egg-info/
